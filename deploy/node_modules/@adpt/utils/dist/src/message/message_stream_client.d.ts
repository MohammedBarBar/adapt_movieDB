/// <reference types="node" />
import stream from "stream";
import { MessageClient, MessageEmitter, TaskEmitter } from "./common";
export interface MessageStreamClientOptions {
    inputStream?: stream.Readable;
    outStream?: stream.Writable;
    errStream?: stream.Writable;
}
export declare class MessageStreamClient implements MessageClient {
    readonly isMessageClient: true;
    outStream?: stream.Writable;
    errStream?: stream.Writable;
    private emitters;
    constructor(options?: MessageStreamClientOptions);
    fromStream(input: stream.Readable): void;
    readonly info: MessageEmitter;
    readonly warning: MessageEmitter;
    readonly error: MessageEmitter;
    readonly task: TaskEmitter;
    private inputMessage;
    private emitter;
}
//# sourceMappingURL=message_stream_client.d.ts.map