export declare type RetryDelay = (attempt: number) => number;
export interface RetryDelayOptions {
    cancel: () => boolean;
    factor: number;
    maxTimeoutMs: number;
    minTimeoutMs: number;
    randomize: boolean;
    retries: number;
}
export declare function rand(min: number, max: number): number;
/**
 * Makes a retry delay function that, given a retry attempt number, returns
 * how many milliseconds to wait before the next attempt.
 * @remarks
 *
 * @returns The number of milliseconds to wait before the next attempt or
 * -1 to indicate that no more retries should be attempted.
 */
export declare function makeRetryDelay(options?: Partial<RetryDelayOptions>): RetryDelay;
//# sourceMappingURL=retries.d.ts.map