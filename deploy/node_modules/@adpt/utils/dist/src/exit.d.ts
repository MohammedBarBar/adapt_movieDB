import { MaybePromise } from "./common_types";
export declare type ExitHandler = (signal: string, details?: object | number) => MaybePromise<void | any>;
export declare type RemoveHandler = () => void;
/**
 * Register a callback that will get called no matter how the process exits.
 * @remarks
 * If `handler` returns a `Promise`, exit will be delayed until the promise
 * resolves.
 */
export declare function onExit(handler: ExitHandler): RemoveHandler;
//# sourceMappingURL=exit.d.ts.map