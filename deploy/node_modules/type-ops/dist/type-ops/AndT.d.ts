/**
 * Logical "and" of `T` and `U`.
 * Distributes over unions.
 */
export declare type AndT<T extends boolean, U extends boolean> = T extends false ? false : U extends false ? false : true;
//# sourceMappingURL=AndT.d.ts.map